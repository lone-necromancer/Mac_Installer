on:
  pull_request:
    branches:   
      - main
      - develop
      
jobs:
  ci-test-checklist:
    name: normal CI checks
    runs-on: macos-latest

    steps: 
      - name: happy flow
        uses: actions/checkout@v4
        run: ./brew_installer.sh
        
 
